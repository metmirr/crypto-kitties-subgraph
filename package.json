{
  "name": "crypto-kitties-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ metmirr/crypto-kitties-subgraph",
    "create-local": "graph create --node http://localhost:8020/ metmirr/crypto-kitties-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ metmirr/crypto-kitties-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 metmirr/crypto-kitties-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.17.1",
    "@graphprotocol/graph-ts": "0.17.0",
    "keytar": "^5.4.0"
  }
}
